import React from 'react'
import DisplaySelection from './DisplaySelection'

export default {
  component: DisplaySelection,
  title: 'DisplaySelection',
}

export const simple = () => <DisplaySelection />
